@startuml User Logout (GET)

header Campground Booking System Sequence Diagram
footer Page %page% of %lastpage%
title "User Logout (GET)"

participant "Client" as client
participant "<<javaScript>>\n:server" as server
participant "<<router>>\n:auth" as routerAuth
participant "<<controllers>>\n:auth" as controllersAuth

client->server ++:req.get('/api/v1/auth/logout')
server->routerAuth ++:app.use('/api/v1/auth', auth)

routerAuth -> controllersAuth ++:logout()

controllersAuth -> client --:response (200, success: true, data: {}, clear cookie)

@enduml